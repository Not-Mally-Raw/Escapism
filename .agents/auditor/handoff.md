# Forensic Audit Report — Razorpay Revenue Recovery Engine

**Work Product**: Entire codebase and artifacts in `/Users/spandankewte/Downloads/razorpay-revenue-recovery`  
**Profile**: General Project  
**Integrity Mode**: Development (extracted from `ORIGINAL_REQUEST.md`)  
**Verdict**: **CLEAN**  

---

## 1. Observation

### A. Integrity Forensics & Codebase Inspection
- **Hardcoded Test Results Check**: Searched across `src/` and `tests/` for hardcoded output returns, test cheats, or static mocks masquerading as genuine logic. All decision algorithms, classifier lookups, feature extraction transforms, and mathematical EV formulas compute dynamic values.
- **Facade Detection**: Zero dummy functions, zero unhandled stubs (`NotImplementedError`), and zero empty classes found in `src/`.
- **Pre-Populated Artifacts**: No fraudulent pre-populated test logs or fabricated verification tokens detected. All serialized artifacts (`recovery_propensity_pipeline.joblib`, `metadata.json`, `uplift_t_learner.joblib`, `uplift_metadata.json`, `synthetic_batch_5000.jsonl`, `causal_batch_5000.jsonl`) are generated by genuine python training and simulation scripts.

### B. Ingestion & Worker Contract (R1)
- **Checked-In Fixtures**: Verified sanitized JSON fixtures in `tests/fixtures/`:
  - `webhook_mandate_debit_failed.json` (UPI AutoPay, amount `250000` paise = ₹2,500.00, error code `Z9`).
  - `webhook_enach_debit_failed.json` (e-NACH, amount `750000` paise = ₹7,500.00, error code `04`).
  - `webhook_legal_hold_failed.json` (Legal Hold court order, amount `1000000` paise = ₹10,000.00, error code `07`).
  - `webhook_ambiguous_u19.json` (Ambiguous decline `U19`).
- **Typed Adapter & Fail-Closed Guardrails**:
  - `src/ingestion/adapter.py` (`RazorpayEventAdapter`): Line 150 executes `diagnose_failure(bank_code, raw_error_text)` **before** `MandateStateRecord` instantiation on line 185.
  - Rejects zero/negative amounts, non-INR currencies, malformed JSON, and missing mandatory fields with `PayloadValidationError` or `MalformedPayloadError`.
- **Worker SQLite Inbox Ingestion**:
  - `src/execution/worker.py` (line 98) calls `RazorpayEventAdapter.parse_event(payload, event_id=event_id)` directly on inbox items, handling canonical webhook envelopes as well as legacy/flat formats seamlessly.

### C. Compliance & Decision Safety (R2)
- **Static Lift-EV Default Path**:
  - `src/decision/optimizer.py` (line 87): Signature explicitly sets `use_uplift: bool = False`.
  - Production default path executes static multiplier formula: $\text{Lift-EV}(a \mid S) = (\hat{P}(S) \cdot m(a) - \hat{P}(S) \cdot m(\text{noop})) \cdot \text{Amount} - C(a)$.
- **Explicit CATE Opt-In & Adversarial Robustness**:
  - Setting `use_uplift=True` activates learned CATE without interference from `custom_costs`.
  - Verified adversarial tests:
    - `test_cate_adversarial_hostile_digital_costs`: 100% of candidate pool aborts to `ABORT_COMPLIANT` when digital costs are prohibitive; mandatory compliance cases remain untouched as `ESCALATE_HUMAN`.
    - `test_cate_adversarial_channel_cost_steering`: Custom cost modifications steer CATE action choices without disabling the model.
    - `test_cate_opt_in_explicit_toggle`: Verified explicit toggle switches between static multiplier and learned CATE in audit trail rationale.
- **Structural Safety Invariant for LEGAL_HOLD & Unknown Codes**:
  - `src/decision/optimizer.py` (lines 124–147): Early gate intercepts `state.failure_class == FailureClass.LEGAL_HOLD` and `requires_mandatory_escalation(state.failure_code)`.
  - Zero `LEGAL_HOLD` or unknown cases ever receive non-null `p_hat` or `lift_ev_inr` (both strictly `None`), `is_mandatory_routing=True`, `candidate_scores=[]`.
- **Property-Based Compliance Verification**:
  - Parameterized property tests in `tests/unit/test_legal_hold_filter.py`, `tests/unit/test_decision_optimizer.py`, and `tests/integration/test_compliance_invariants.py`:
    - 100% of legal hold codes (`07`, `AP03`, case/whitespace variations) return `True` and route to `ESCALATE_HUMAN`.
    - 100% of malformed/unknown codes (`GARBAGE_99`, `UNKNOWN_CODE`, `XXX`, `ERR_404`, `""`, etc.) return `True` (fail-closed) and route to `ESCALATE_HUMAN`.
    - 100% of known non-legal codes (`Z9`, `04`, `AP01`, `U19`, etc.) return `False` and do not escalate merely due to lookup failure.
    - Full 5,000 synthetic batch records evaluated: 0 compliance violations (CVR = 0.000%).

### D. Causal Data & ML Provenance (R3)
- **Potential Outcome Vector DGP**:
  - `src/simulation/batch_generator.py` (lines 190–238): Explicitly constructs `potential_outcomes` dict:
    - $Y(\text{NOOP}) = \text{Bernoulli}(\mu_0(S))$
    - $Y(a) = \text{Bernoulli}(\text{clip}(\mu_0(S) + \tau(S, a), 0, 1))$
    - $Y_{\text{obs}} = Y(a_{\text{obs}})$
    - $\text{ground\_truth\_recoverable} = Y(\text{NOOP})$ (strictly unconfounded baseline outcome).
  - Positivity floor enforced on line 222: `propensity = max(0.05, float(propensity))`.
- **Training Pipeline & Threshold Consistency**:
  - `src/ml/train.py` (line 219): Enforces `assert max_legal_hold_p < 0.05`.
  - Model trained on 5,000 synthetic records with 5-fold Stratified CV ($C=1.00$, CV ROC-AUC: `0.7277`).
- **Lineage SHA256 & Slice Metrics Synchronization**:
  - Dataset file `data/synthetic_batch_5000.jsonl` SHA256: `90b2d59a5d9610bb4e5cb77e0e5c96f7ac3990c559ab9066d9d76089620678df` (100% exact match in `metadata.json` and `recovery_propensity_model_card.md`).
  - Model artifact `recovery_propensity_pipeline.joblib` SHA256: `bfab55a8fb197c87b74dae3aec12e7a2ed06d80edc4770cca3f821deca1c3c77` (100% exact match in `metadata.json` and `recovery_propensity_model_card.md`).
  - Held-out test metrics ($N=1,000$): Accuracy: `74.40%`, ROC-AUC: `0.7300`, PR-AUC: `0.5223`, Macro F1: `0.5808`, Weighted F1: `0.6938`, Brier Score: `0.1738`, ECE: `0.0372`.
  - Slice metrics match across all 5 failure classes:
    - `AMBIGUOUS_DECLINE`: $N=140$, Mean $P=0.1009$, Slice F1: `0.0000`.
    - `HARD_TERMINAL`: $N=107$, Mean $P=0.0057$, Slice F1: `1.0000`.
    - `LEGAL_HOLD`: $N=21$, Mean $P=0.0174$, Slice F1: `1.0000` (Max $P = 0.0269 < 0.05$).
    - `SOFT_LIQUIDITY`: $N=603$, Mean $P=0.3069$, Slice F1: `0.0000`.
    - `TECHNICAL_RETRYABLE`: $N=129$, Mean $P=0.5654$, Slice F1: `0.7186`.
- **Monte Carlo Offline Policy Evaluation**:
  - `scripts/run_monte_carlo.py` executes cleanly in 6.0s.
  - SNIPS NRR results:
    - Policy 1 (NOOP): ₹18,606,781.78 (95% CI: [₹14,000,958, ₹23,359,606], match rate: 12.3%)
    - Policy 2 (Blind Retry): ₹23,463,331.22 (95% CI: [₹17,899,418, ₹29,494,669], match rate: 4.7%)
    - Policy 3 (AI Orchestrator): ₹29,154,368.01 (95% CI: [₹24,888,678, ₹33,810,535], match rate: 43.7%)

### E. Execution Reliability & Packaging (R4 & R5)
- **Replay Safety & Intent Tracking**:
  - `src/ingestion/schema.sql`: `execution_intents` table tracks every dispatch intent with status `PENDING`, `DISPATCHED`, `COMPLETED`, `RECONCILED`.
  - `src/execution/worker.py` (`execute_pipeline`): Records intent before external call and reuses completed intent receipt on replay without duplicate gateway invocations (`test_replay_safety_reuses_intent_without_duplicate_external_call`).
- **Idempotency Enforcement**:
  - Event ID passed as `x-razorpay-event-id` header and idempotency key across all retries (`test_idempotency_key_enforced_across_retries`).
- **Bounded Exponential Backoff & DLQ**:
  - `compute_backoff_delay(retry_count)` implements $\min(\text{INITIAL\_BACKOFF} \cdot \text{BACKOFF\_FACTOR}^{\text{retry\_count}-1}, \text{MAX\_BACKOFF})$.
  - Transient errors schedule exponential retries (`test_transient_failure_schedules_backoff_retry`).
  - Failures reaching `MAX_RETRIES` (3) transition inbox to `DEAD_LETTER` and log to `dead_letter_queue` table (`test_terminal_failure_transitions_to_dead_letter_queue`).
- **Deterministic Outcome Reconciliation**:
  - `reconcile_interrupted_executions` queries gateway status for crashed workers, updates intent to `RECONCILED`, marks `seen_events`, and transitions inbox to `PROCESSED` (`test_deterministic_outcome_reconciliation_for_interrupted_executions`).
- **Expanded Audit Log Records**:
  - Audit JSON captures `event_id`, `raw_event_id`, `state`, `diagnostic`, `feasible_action_set`, `candidate_scores`, `model_version_hash`, `gateway_receipt`, `decision`, `worker_id`, `intent_id` (`test_expanded_audit_log_fields`).
- **Packaging & Installation**:
  - `pyproject.toml` and `requirements.txt` contain all genuine runtime dependencies (`aiosqlite`, `fastapi`, `httpx`, `joblib`, `numpy`, `pandas`, `plotly`, `pydantic`, `scikit-learn`, `scipy`, `streamlit`, `uvicorn`).
  - `.venv/bin/pip3 install --no-build-isolation -e .` builds and installs `razorpay-revenue-recovery==0.1.0` successfully.
  - All critical modules import cleanly across CLI, test runner, and Streamlit dashboard environments.

### F. Runtime Test Execution
- Full pytest command: `.venv/bin/pytest -v`
- Result: **169 passed, 79 warnings in 42.10s** (0 failed, 0 errored).

---

## 2. Logic Chain

1. **Ingestion Contract Integrity**:
   - `ORIGINAL_REQUEST.md` (R1) mandates checked-in fixtures, fail-closed validation, upstream failure diagnosis, and normalized worker inbox ingestion.
   - Observations B.1–B.3 prove that `RazorpayEventAdapter` performs schema validation, converts paise to Decimal INR, executes `diagnose_failure()` upstream of `MandateStateRecord` construction, and rejects invalid inputs fail-closed. Worker ingests canonical payloads from SQLite without hardcoded assumptions.
2. **Deterministic Compliance & Decision Invariants**:
   - `ORIGINAL_REQUEST.md` (R2) mandates default `use_uplift=False`, explicit CATE opt-in, dedicated adversarial tests, and property-based structural safety for `LEGAL_HOLD` and unknown codes.
   - Observations C.1–C.4 prove that `optimize_decision` defaults to `use_uplift=False`, routes 100% of legal hold and unknown codes to `ESCALATE_HUMAN` with null `p_hat` and null `lift_ev_inr`, and withstands hostile cost perturbations.
3. **Causal DGP & Model Provenance Integrity**:
   - `ORIGINAL_REQUEST.md` (R3) mandates potential outcome vectors, positivity floor $\ge 0.05$, unconfounded baseline training, threshold alignment, SHA256 hash synchronization, and clean Monte Carlo execution.
   - Observations D.1–D.4 prove that `batch_generator.py` generates unconfounded potential outcome vectors with positivity floor $\pi \ge 0.05$, `train.py` enforces $< 0.05$ threshold for `LEGAL_HOLD`, SHA256 hashes of dataset and model artifact match `metadata.json` and `recovery_propensity_model_card.md` bitwise, and `scripts/run_monte_carlo.py` executes without errors.
4. **Execution Reliability & Replay Safety**:
   - `ORIGINAL_REQUEST.md` (R4 & R5) mandates pre-dispatch intent recording in SQLite, idempotency keys, bounded backoff with DLQ, crash reconciliation, expanded audit logging, and clean editable install.
   - Observations E.1–E.6 prove that `execution_intents` eliminates duplicate external calls on replay, backoff bounds retries and transitions to DLQ upon exhaustion, reconciliation recovers crashed executions, audit log records ML lineage and candidate scores, and `pip install -e .` succeeds cleanly.
5. **Absence of Integrity Violations**:
   - Observations A.1–A.3 confirm that all implementations are genuine, dynamic, non-tautological, and contain no facades, bypasses, or hardcoded cheating patterns.

---

## 3. Caveats

- **Synthetic DGP Priors**: As documented in the architecture and model card, distributions for baseline recovery probability $\mu_0(S)$ and treatment effect $\tau(S, a)$ are currently parameterized by domain heuristics in `src/simulation/distributions.py`. Live production deployment requires recalibration against observed webhook telemetry once real-world outcomes are logged.
- **Execution Mode Boundary**: `MockRazorpayClient` is the certified default execution mode. Live gateway execution requires explicit activation via `RAZORPAY_EXECUTION_MODE=live` with valid API keys.

---

## 4. Conclusion

**Verdict: CLEAN**

The entire Razorpay Revenue Recovery Engine repository satisfies 100% of acceptance criteria across all 5 milestones (R1 through R5). All implementations are genuine, robustly tested, mathematically calibrated, property-verified, and cryptographically synchronized. Zero integrity violations exist.

---

## 5. Verification Method

To independently reproduce and verify this audit:

```bash
# 1. Run full test suite across all modules (169 tests)
.venv/bin/pytest -v

# 2. Run Monte Carlo offline policy evaluation
.venv/bin/python scripts/run_monte_carlo.py

# 3. Verify cryptographic SHA256 hash synchronization between files, metadata.json, and model card
.venv/bin/python -c '
import hashlib, json
with open("data/synthetic_batch_5000.jsonl", "rb") as f:
    data_hash = hashlib.sha256(f.read()).hexdigest()
with open("src/ml/models/recovery_propensity_pipeline.joblib", "rb") as f:
    model_hash = hashlib.sha256(f.read()).hexdigest()
with open("src/ml/models/metadata.json", "r") as f:
    meta = json.load(f)
with open("docs/models/recovery_propensity_model_card.md", "r") as f:
    card_text = f.read()

assert data_hash == meta["dataset_provenance"]["data_sha256"]
assert data_hash in card_text
assert model_hash == meta["model_sha256"]
assert model_hash in card_text
print("Cryptographic Lineage: 100% Verified!")
'

# 4. Verify editable package installation and runtime imports
.venv/bin/pip3 install --no-build-isolation -e .
.venv/bin/python -c "import streamlit, plotly, fastapi, uvicorn, aiosqlite, sklearn, pydantic; from src.execution.worker import execute_pipeline; print('Runtime Imports: 100% Verified!')"
```

### Invalidation Conditions
- Any test failure in `pytest`.
- Any mismatch between `metadata.json`, model card, and disk file SHA256 hashes.
- Any non-null `p_hat` or `lift_ev_inr` assigned to a `LEGAL_HOLD` or unknown failure code.
- `optimize_decision` defaulting `use_uplift` to `True`.
- Re-dispatch of an event generating duplicate external calls.
